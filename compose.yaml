services:
  todolist-api:
    build: .
    image: todolist-api
    ports:
      - '5014:8080'
    env_file:
      - .env
    depends_on:
      - postgres

  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    env_file:
      - .env
